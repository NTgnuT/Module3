package LT.DemoStack;

import java.util.*;

public class Demo {
    public static void main(String[] args) {

        // array.offer(9): thêm 1 phần tử vào cuối mảng;

        // array.poll(): lấy phần tử đầu tiên và xóa nó đi. Nếu mảng rỗng thì trả về mảng rỗng ;
        // array.remove(): xóa phần tử đầu tiên, mảng rỗng trả về null;

        // array.peek(): trả về phần tử đầu những không xóa, mảng rỗng trả về null;
        // array.element(): trả về phần tử đầu không xóa, mảng rỗng trả về mảng rỗng;

        PriorityQueue<String> queue = new PriorityQueue<>();

        Queue<String> queue1 = new LinkedList<>() ;
    }

}
